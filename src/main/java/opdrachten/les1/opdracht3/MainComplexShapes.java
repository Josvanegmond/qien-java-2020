package opdrachten.les1.opdracht3;

/**
 * Opdracht 1.3:
 * Using multiple existing classes
 */
public class MainComplexShapes {

    // TODO: create a ComplexShapeCalculator class
    //  - Add methods that calculate the area and edge size of complex shapes
    //  - The methods have a size argument and a boolean to determine if they return
    //  - Use the utility classes directly from package "opdracht2"
    //  - Expand them with more primitive shapes as you see fit
    //  - Shapes:
    //  - A heart made from a diamond shape and two half-circles
    //  - A snowman made of three stacked circles, each a quarter smaller than the previous one

    public static void main(String[] args) {

//         TODO: uncomment if the methods are created
//        double heartArea = getHeartArea(5);
//        double heartEdge = getHeartEdge(5);

//         FIXME: ? should be replaced by proper values.
        System.out.println("Heart of size ? has an area of ? and an edge length of ?.");

//         TODO: uncomment if the methods are created
//        double snowmanArea = getSnowmanArea(8);
//        double snowmanEdge = getSnowmanEdge(8);

//         FIXME: ? should be replaced by proper values.
        System.out.println("Snowman of base size ? has an area of ? and an edge length of ?.");
    }
}
