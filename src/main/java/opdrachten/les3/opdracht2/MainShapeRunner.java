package opdrachten.les3.opdracht2;

import java.util.ArrayList;

/**
 * Opdracht 2:
 * Create juggle balls!
 */

public class MainShapeRunner {

    public static void main(String[] args) {
//        TODO: Create a JuggleBall class that implements DrawableCircle
//          - Add a JuggleBall objects to juggleBallList
        ArrayList<DrawableCircle> juggleBallList = new ArrayList<>();

//        FIXME: uncomment the following method and resolve the error
//        ShapeRunner.start(juggleBallList);

//        TODO: After a green juggleball shows in the window, add four more juggleballs
//          - each JuggleBall must have a different position and color

//        TODO: let JuggleBall implement java.lang.Runnable interface
//          - in the run method, modify the x and y attributes by a small number to make the ball move
//          - try to make the ball move in circles using the formula:
//              - x = startX + Math.cos(angle) * radius
//              - y = startY + Math.sin(angle) * radius
//              - angle += 0.01;
    }
}
